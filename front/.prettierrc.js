module.exports = {
	// 문자열은 작은 따옴표(')로 formatting
	singleQuote: true,
	// 코드 맨 뒤에 세미콜론 추가
	semi: true,
	// 객체, 배열의 요소 맨 뒤에 쉼표(,)를 추가
	trailingComma: 'all',
	// 탭 사용을 활성화
	useTabs: true,
	// 화살표 함수가 매개변수를 받을 때 항상 괄호를 추가
	arrowParens: 'always',
	// 들여쓰기 너비를 2칸으로 지정
	tabWidth: 2,
	// 코드 한 줄의 최대 길이를 80으로 지정
	printWidth: 80,
};
